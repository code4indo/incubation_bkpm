# Geospatial Data Pipeline — Python Dependencies
# Install: pip install -r requirements.txt

requests>=2.28.0      # HTTP downloads + REST API queries
pyshp>=2.3.0          # Shapefile reading
pyyaml>=6.0           # YAML catalog parsing
