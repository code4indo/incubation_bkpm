# API Service Requirements
# Install: pip install -r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-multipart>=0.0.9

# For CORS support (already included in FastAPI but good to be explicit)
# starlette>=0.36.0 (included with fastapi)

# Optional: for production deployment
gunicorn>=21.2.0

# Optional: for database (if migrating from JSON to PostgreSQL)
# sqlalchemy>=2.0.0
# asyncpg>=0.29.0
# alembic>=1.13.0

# Optional: for caching
# redis>=5.0.0

# Optional: for monitoring
# prometheus-client>=0.19.0
